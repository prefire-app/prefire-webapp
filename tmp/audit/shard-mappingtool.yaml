```yaml
findings:
  - id: mt-001
    severity: P0
    category: security
    file: src/components/MappingTool.tsx:252-256
    evidence: |
      const params = new URLSearchParams({
        where: `STATE='${sel.stateFips}' AND COUNTY='${sel.countyFips}'`,
        ...
      });
      const res = await fetch(`${TIGERWEB_URL}?${params}`);
    fix: |
      Use URLSearchParams correctly: pass an object with key-value pairs, not a pre-formatted where clause.
      The FIPS codes are injected directly into SQL without escaping. Consider using the API's parameterized query
      endpoint or at minimum URL-encode the where parameter with proper SQL escaping.
    effort: small
    cross_file: []

  - id: mt-002
    severity: P1
    category: correctness
    file: src/components/MappingTool.tsx:243-256
    evidence: |
      const handleMoveMap = (lat: number, lng: number) => {
        if (mapRef.current) {
          mapRef.current.setView([lat, lng], 17);
        }
      };
      ...
      <AddressSearchPopup onSearch={handleMoveMap} />
      AND also calls fetch(TIGERWEB_URL...) without AbortController
    fix: |
      Add AbortController to the fetch call in the StateCountySelector handler (lines 259-273).
      Prevent hung requests on unmount or rapid state changes. Set a 10s timeout.
    effort: small
    cross_file: []

  - id: mt-003
    severity: P1
    category: correctness
    file: src/components/AddressSearchPopup.tsx:13-23
    evidence: |
      const res = await fetch(url);
      const data = await res.json();
      if (data && data.length > 0) {
        const { lat, lon } = data[0];
        onSearch(parseFloat(lat), parseFloat(lon));
    fix: |
      Add AbortController to fetch. Validate Nominatim response structure before destructuring.
      Check that parseFloat() returns a valid number. Add error message if fetch fails.
      onSearch(parseFloat(lat), parseFloat(lon)) should guard against NaN.
    effort: small
    cross_file: []

  - id: mt-004
    severity: P1
    category: correctness
    file: src/components/MappingTool.tsx:88-92
    evidence: |
      function ZoomTracker({ onZoomChange }: { onZoomChange: (zoom: number) => void }) {
        const map = useMap();
        useMapEvents({ zoomend: () => onZoomChange(map.getZoom()) });
        useEffect(() => {
          onZoomChange(map.getZoom());
        }, [map, onZoomChange]);
    fix: |
      Both useMapEvents and useEffect trigger onZoomChange on mount/zoom. This causes a double-call.
      Remove the useEffect. If onZoomChange needs to be called on initial mount, call it once inside useMapEvents
      with a flag or replace useEffect with a one-time useLayoutEffect.
    effort: small
    cross_file: []

  - id: mt-005
    severity: P1
    category: correctness
    file: src/components/MappingTool.tsx:107-145
    evidence: |
      function LeafletDrawControls({ onPolygonDrawn, onClearRef }: { ... }) {
        ...
        function handleCreated(event: any) {
          const layer = (event as any).layer;
          drawnItemsRef.current.addLayer(layer);
          const geojson = layer.toGeoJSON();
          onPolygonDrawn(geojson);
        }
        map.on(L.Draw.Event.CREATED, handleCreated);
        return () => {
          map.off(L.Draw.Event.CREATED, handleCreated);
          ...
    fix: |
      The handleCreated closure captures onPolygonDrawn. If parent re-renders and onPolygonDrawn is not memoized,
      the old reference persists. Wrap onPolygonDrawn with useCallback in parent, or use useRef + useEffect pattern here.
      Type event properly: use L.DrawEvents.Created from leaflet-draw types.
    effort: medium
    cross_file: [src/components/MappingTool.tsx:189]

  - id: mt-006
    severity: P1
    category: correctness
    file: src/components/MappingTool.tsx:260-273
    evidence: |
      const walk = (node: any) => {
        if (Array.isArray(node) && typeof node[0] === "number") {
          flat.push(node as number[]);
        } else if (Array.isArray(node)) {
          node.forEach(walk);
        }
      };
      walk(coords);
      if (flat.length > 0) {
        const lngs = flat.map((c) => c[0]);
        const lats = flat.map((c) => c[1]);
    fix: |
      No validation that coords are valid GeoJSON. If coords is malformed or None, walk() produces an empty flat array
      and centroid falls back to stateCentroid silently. Add explicit validation: ensure coords is a valid GeoJSON
      Polygon/MultiPolygon, extract bounds properly, and guard against NaN in centroid calculation.
    effort: medium
    cross_file: []

  - id: mt-007
    severity: P1
    category: perf
    file: src/components/MappingTool.tsx:189
    evidence: |
      const handlePolygonDrawn = useCallback((geojson: any) => {
        setDrawnPolygons((prev) => [...prev, geojson]);
      }, []);
    fix: |
      geojson parameter is typed as any. More importantly, the callback is only memoized at this level.
      handleMoveMap (line 243) is not memoized and is passed to AddressSearchPopup. If AddressSearchPopup is memo'd,
      it will re-render every time MappingTool re-renders due to handleMoveMap identity change.
      Wrap handleMoveMap with useCallback.
    effort: small
    cross_file: [src/components/MappingTool.tsx:243]

  - id: mt-008
    severity: P2
    category: correctness
    file: src/components/MappingTool.tsx:240-242
    evidence: |
      const handleMoveMap = (lat: number, lng: number) => {
        if (mapRef.current) {
          mapRef.current.setView([lat, lng], 17);
        }
      };
    fix: |
      mapRef could be null or the map could be unmounted when onSearch is called from AddressSearchPopup.
      Add a guard: if (!mapRef.current?.isOpen || !mapRef.current?.getZoom) return early.
      Consider storing map instance in Context to avoid ref chaos.
    effort: small
    cross_file: []

  - id: mt-009
    severity: P2
    category: deps
    file: src/components/MappingTool.tsx:1-16
    evidence: |
      No imports of terra-draw, @react-google-maps/api, @googlemaps/js-api-loader, or proj4 found.
      But package.json likely has these deps. knip + depcheck flagged as unused.
    fix: |
      Remove unused dependencies from package.json:
      @googlemaps/js-api-loader, @react-google-maps/api, terra-draw, terra-draw-google-maps-adapter, proj4
      This reduces bundle size and maintenance burden.
    effort: trivial
    cross_file: []

  - id: mt-010
    severity: P2
    category: a11y
    file: src/components/MappingTool.tsx:319-357
    evidence: |
      <select ... aria-label="Base map layer"> ... </select>
      <button ... aria-label="Open guide"> ... </button>
      But no aria-label on leaflet-draw polygon tool or edit/delete buttons.
      No focus trap or focus restoration when modals (AnalyzerGuide, StateCountySelector) open/close.
    fix: |
      Add aria-labels to leaflet-draw toolbar buttons or use a Tooltip with aria-describedby.
      Implement focus management: when showGuide/showSelector becomes true, trap focus inside modal.
      On close, return focus to the triggering element (the "?" button).
      Use a library like focus-trap-react or implement manually with a useEffect.
    effort: medium
    cross_file: []

  - id: mt-011
    severity: P2
    category: correctness
    file: src/components/MappingTool.tsx:231-239
    evidence: |
      {drawnPolygons.length > 0 && !showConfirm && (
        <div className="...">
          <span>
            {drawnPolygons.length} {drawnPolygons.length === 1 ? "polygon" : "polygons"} drawn
          </span>
          <button onClick={() => setShowQuestionnaire(true)}>
            Done?
          </button>
    fix: |
      No validation on drawn polygons before showing "Done?" button or allowing submit to ConfirmSubmitPopup.
      Add minimum/maximum vertex count and area validation. Cap at ~1000 vertices per polygon to prevent DoS.
      Warn user if any polygon is self-intersecting or has zero area.
    effort: small
    cross_file: []

  - id: mt-012
    severity: P2
    category: correctness
    file: src/components/MappingTool.tsx:250-275
    evidence: |
      catch {
        // Fall back to state centroid on failure
        setMapCenter(stateCentroid);
        if (mapRef.current) {
          mapRef.current.setView(stateCentroid, 8);
        }
      }
    fix: |
      Silent error handling. Network errors, invalid FIPS codes, or malformed GeoJSON responses are caught
      but never surfaced to user. No error message, no retry button, no console.error(). Add error state
      and display a transient toast or error banner.
    effort: small
    cross_file: []

  - id: mt-013
    severity: P2
    category: perf
    file: src/components/MappingTool.tsx:252-273
    evidence: |
      const centroid = [
        (Math.min(...lats) + Math.max(...lats)) / 2,
        (Math.min(...lngs) + Math.max(...lngs)) / 2,
      ];
      setMapCenter(centroid);
    fix: |
      Centroid calculation runs every time StateCountySelector onConfirm is called. Not memoized.
      If data is large (many vertices), this becomes expensive. Compute centroid inside useMemo or
      move to a utility function with memoization. Low priority but visible on large polygons.
    effort: small
    cross_file: []

  - id: mt-014
    severity: P2
    category: correctness
    file: src/components/MappingTool.tsx:50-79
    evidence: |
      const PMTilesBuildingsLayer = ({ stateFips, visible }: ...) => {
        const map = useMap();
        const layerRef = useRef<any>(null);
        useEffect(() => {
          if (!visible || !stateFips) return;
          layerRef.current = leafletLayer({ url, ... });
          layerRef.current.addTo(map);
          return () => {
            if (layerRef.current) {
              map.removeLayer(layerRef.current);
              layerRef.current = null;
            }
          };
        }, [map, visible, stateFips]);
    fix: |
      Type layerRef as L.Layer | null instead of any. More critically, if the leafletLayer constructor
      throws or returns null, the code silently continues. Add error handling and type narrowing.
    effort: small
    cross_file: []

  - id: mt-015
    severity: P2
    category: correctness
    file: src/components/AddressSearchPopup.tsx:17-20
    evidence: |
      const { lat, lon } = data[0];
      onSearch(parseFloat(lat), parseFloat(lon));
      onClose();
    fix: |
      Nominatim lat/lon are strings. parseFloat can return NaN if strings are malformed.
      onSearch is called with potentially NaN values. Add validation: 
      const latNum = parseFloat(lat); if (!isFinite(latNum)) { alert(...); return; }
    effort: trivial
    cross_file: []
```